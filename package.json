{
  "name": "aigame-hello",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
