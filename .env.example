# 本地开发：若希望用 Vite 代理代替浏览器直连（与 .env.local 一起用）
# VITE_USE_OPENROUTER_PROXY=true
# OPENROUTER_API_KEY=sk-or-...

# 站点不在域名根目录而在子路径时（如 https://example.com/aigame/）
# VITE_BASE_PATH=/aigame/

# 本地仍可直接在页面里填 OpenRouter 密钥（不设置上述两项时）
