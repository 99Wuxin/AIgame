# Cloudflare Pages — 使用 `wrangler pages deploy`，不要用 `wrangler deploy`（那是 Workers）。
# CI 部署命令示例：npm run deploy 或 npx wrangler pages deploy dist
# 构建：npm run build → dist；根目录 functions/ 随 Pages 发布
# Secrets：OPENROUTER_API_KEY（Pages → Variables and Secrets）

name = "aigame"
compatibility_date = "2024-11-25"
pages_build_output_dir = "dist"
