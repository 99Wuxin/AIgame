# Cloudflare Pages：构建输出 dist，Functions 位于 /functions
# 部署后在 Pages → Settings → Variables and Secrets 中设置：
# - Secret: OPENROUTER_API_KEY
# - 构建变量: VITE_USE_OPENROUTER_PROXY=true（见 README）

name = "aigame"
compatibility_date = "2024-11-25"
pages_build_output_dir = "dist"
